# Python dependencies for Ch.3's scripts/ (traffic generation + pcap analysis).
#
# NOT covered here (system-level — see README.md "Setup"):
#   - p4c (P4 compiler), BMv2 / simple_switch, Mininet

scapy==2.5.0             # ddosd_t header (de)serialization in scripts/main.py, packet crafting in traffic.py
